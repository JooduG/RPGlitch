{
  "name": "rpglitch",
  "version": "1.0.0",
  "description": "RPGlitch - AI-powered storytelling platform for Perchance with comprehensive rules system for development",
  "main": "build/scripts/build-perchance.js",
  "scripts": {
    "preflight": "npm run build && npm run lint && echo \"Preflight check complete (tests and typecheck not configured).\"",
    "build": "node build/scripts/build-perchance.js",
    "build:copy": "node build/scripts/build-and-copy.js",
    "build:watch": "nodemon --watch apps --ext html,js,css --exec 'node build/scripts/build-perchance.js'",
    "lint": "npx eslint 'apps/**/*.js' 'tests/**/*.js' 'build/scripts/**/*.js' --config eslint.config.mjs && stylelint 'apps/**/*.{css,scss}' --config stylelint.config.js && htmlhint 'apps/rpglitch/**/*.html' --config .htmlhintrc",
    "lint:fix": "npx eslint 'apps/**/*.js' 'tests/**/*.js' 'build/scripts/**/*.js' --config eslint.config.mjs --fix && stylelint 'apps/**/*.{css,scss}' --config stylelint.config.js --fix && htmlhint 'apps/rpglitch/**/*.html' --config .htmlhintrc --fix",
    "validate": "node -e \"require('./build/scripts/build-perchance.js').validateOutputFile()\"",
    "clean": "rm -rf build/output/",
    "deploy": "npm run build && echo '✅ Build complete! Copy build/output/RPGlitch-perchance.html to Perchance.'",
    "rules:update": "echo 'Use \"update memory bank\" to refresh memory bank files'",
    "test": "npx jest",
    "postinstall": "chmod +x node_modules/.bin/jest || true"
  },
  "keywords": [
    "perchance",
    "rpglitch",
    "ai",
    "storytelling",
    "generator"
  ],
  "author": "Glitch",
  "license": "MIT",
  "devDependencies": {
    "cssnano": "^6.1.2",
    "eslint": "^9.0.0",
    "html-minifier-terser": "^7.2.0",
    "htmlhint": "^1.1.4",
    "jest": "^29.7.0",
    "jsdom": "^24.0.0",
    "nodemon": "^3.0.0",
    "postcss": "^8.4.35",
    "postcss-cli": "^10.1.0",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.1",
    "terser": "^5.30.3",
    "jest-environment-jsdom": "^29.7.0"
  },
  "dependencies": {
    "sass": "^1.89.2"
  }
}