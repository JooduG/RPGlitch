# Reference: Antigravity Design Mappings

When translating concepts, strictly map them to these physical realities:

- "Stateful variable" -> `let count = $state(0);`
- "Derived value" -> `let doubled = $derived(count * 2);`
- "Background color" -> `background: var(--color-chalk-surface);`
- "Text color" -> `color: var(--color-chalk-foreground);`
- "Subtle border" -> `border: 1px solid var(--color-chalk-border);`
- "Primary Button" -> `background: var(--color-chalk-accent); color: var(--color-chalk-surface);`
