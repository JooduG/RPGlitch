# Workflow: Edit Design MD

1. Read the current UI state from `.agent/state/design.md`.
2. Apply requested changes using ONLY Chalk variables and Svelte 5 concepts.
3. Overwrite `.agent/state/design.md` with the updated spec.
