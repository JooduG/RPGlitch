# Gemini CLI Operational Protocol

This file instructs the Gemini CLI agent to use the universal agent protocol.

**Primary Protocol:** See `AGENTS.md` in the repository root.

All operational rules, principles, and guidelines are defined in `AGENTS.md`. This ensures a single source of truth for all agent behavior, regardless of the specific AI environment being used.