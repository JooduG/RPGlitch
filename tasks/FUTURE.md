# 📜 FUTURE (The Muscle)

> **Role**: Active implementation blueprint for the _current_ track.
> **Status**: Track Initialized — `refactor-ui-deconstruction-2026-08-08` [~~]
> **Tracks**: `refactor-ui-deconstruction-2026-08-08` [~~] · `forge-entity-isolation-2026-08-03` ✅ · `memory-engine-tuning-2026-08-03` ✅ · `engine-stability-fixes-2026-08-03` ✅

---

## 🎯 Active Goal

Two coordinated workstreams on the live engine:

1. **Memory Engine & Temporal Mechanics** — recalibrate recency-vs-semantic scoring so relevant old memories are not drowned out, upgrade the embedding cache to a true bounded LRU with a higher cap, and persist vector embeddings so cold loads stop re-inferring.
2. **Engine Bugs & System Stability** — stop the LLM output cleaner from stripping legitimate closing dialogue quotes, and make Dexie `versionchange` reloads graceful so active runtime session pointers survive.

All changes are source-tree edits in `src/`. The user runs `npm run verify` + `npm run build` locally; every modified file is handed off for download.

---

## 🎯 Active Track — forge-entity-isolation-2026-08-03

Two coordinated fixes on the live memory engine:

1. **Entity-Specific Memory Forge Isolation** — `forge_memory()` now takes the full set of active entity targets, and the Memory Forge prompt renders every entity's own eternal/present context, instructing the LLM to write ONE memory per entity from that entity's own perspective. `consolidate()` routes each entity's OWN memory to that entity only — no more duplicating a single shared character vector across AI Character, User Persona, and Fractal.
2. **Forge Schema Vector Typing (`VE`)** — `MEMORY_JSON_SCHEMA.memories[*]` gains `"type": "past | future | present"`. Consolidated memories route: `past` → `entity.past` (historical anchor), `future` → `entity.future` (prophecy/intent, resolved later), `present` → merged into `entity.present.non_physical` (immediate directive). Legacy single-`directive` LLM responses still fall back to the old shared-memory behavior.

**Files**: `src/intelligence/temporal.js`, `src/intelligence/prompts.js` (+ `temporal.test.js`, `prompts.test.js`).

**Verified this session**: 16/16 harness assertions passed against the real bundled module (entity isolation, type routing/normalization, present-skip-embedding, legacy fallback, per-entity MEMORY_FORMATION). All modified files attached in the session handoff.

**Follow-up fix (clean_xml stripping)**: local `npm run test` surfaced `prompts.test.js › build_memory_prompt() renders entity-specific forge contexts` — the `<ENTITY_CONTEXT>` blocks were invisible to the test. Root cause: `clean_xml` recursively removes EMPTY XML tags, and entities carrying only a `name` (no eternal/present content yet) rendered as `<AI_CHARACTER name="Viper"></AI_CHARACTER>`, which `clean_xml` stripped entirely. Fix: `render_entity_memory_context()` now always emits a non-empty `<NAME>...</NAME>` child inside each entity block, so the block (and its `name="..."` attribute) always survives `clean_xml`. Verified against the real `prompts.js`: all 5 assertions of the failing test pass.

**Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**Prompt-template quirks — prompt-quirk-fixes-2026-08-03**: per user request, fixes for perchance console findings (issues 1-5 + 8 + related; #6 dropped as impossible). (1) double `<VISUAL_ENGINE>` in image prompts — `optics.js` now strips the preset's own wrapper before composing. (2) POV dedupe — removed `pov_protocol,` from render_character protocols (task `<POV_DIRECTIVE>` is the single authority) and dropped `POV.THIRD_PERSON` from render_narrator protocols (narrator keeps `POV.NARRATOR`; epilogue no longer forces third-person). (3) space/underscore keys (`[DENTAL FEATURES: ...]`) — `text.js` `safe_parse_pseudo_json` + `parser.js` bracketed regex normalize keys, `physical_to_xml` emits underscored tags, premades Nova City keys → `UPPER_CITY`/`LOWER_CITY`/`VISUAL_THEME`, `fragments.js` directives list the new optional keys. (4) `<PRESENT><PHYSICAL>`/`<ETERNAL><PHYSICAL>` nesting applied across director/character/ghostwriter/narrator/entity-memory/enhancement templates (legacy flat `PRESENT_PHYSICAL`/`ETERNAL_PHYSICAL` gone). (5) fractal `certainty`/`regulation` removed via `format_dynamics_attrs(obj, {cognitive:false})` (fractal identities keep only velocity/entropy; characters keep cognitive attrs). (8) `<entry role="AI_CHARACTER" name="Nova City">` — kernel flattening (execute_turn + execute_ghostwriter) preserves `role:"fractal"` so `collapse_history` maps fractal messages to `FRACTAL`. (7) JSON-schema-vs-DB drift: DIRECTOR_JSON_SCHEMA `new_vectors` `weight`→`emotional_weight`, temporal.js create fallback `v.emotional_weight ?? v.weight ?? 5`, PROFILE.SCHEMA past/future wording. **Files**: prompts.js, optics.js, text.js, parser.js, premades.js, kernel.js, temporal.js, protocols.js, fragments.js (+ prompts.test.js, HANDOFF.md, tasks/PRESENT.md). **Verified this session** (no local shell): esbuild-wasm syntax-clean ×10 + bundled runtime smoke assertions on parser/text/prompts/optics — all pass. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**ImagePreview follow-up — list width + edge-to-edge item effects (2026-08-03)**: per user request. (1) The profile-picture `Dropdown` now passes `matchWidth`, so the list renders exactly the trigger's width (`w-(--bits-select-anchor-width)`) instead of the default `+3.5rem` wider. (2) Shared `atoms/Dropdown.svelte`: `Select.Viewport` padding `p-1.5` → `p-0` and `Select.Item` lost its `rounded-lg` — hover/focus/selected brightness highlights are now full-bleed edge-to-edge square rows, clipped to the list container's retained `rounded-xl` + `overflow-hidden`; text inset (`px-3 py-2`) untouched. Applies to every dropdown using the atom (Profile narrative/visual style cards, AudioWing, VisualWing). (3) Adopted the user's manual edit as baseline: grid `gap-2` → `gap-4`, `pt-2` dropped (grid now sits exactly the standard `gap-gap-standard` below the negative-prompt field). Verified: programmatic line diff of the pasted file vs Round 1 found exactly that one manual edit; Svelte 5.56.3 client+server compile 0 warnings/0 errors on both files. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**ImagePreview profile-picture promotion (2026-08-03)**: per user request, the storymode image lightbox (`src/ui/molecules/ImagePreview.svelte`) gained a single combined "Use as Profile Picture" control — a bits-ui `Dropdown` in the right metadata panel whose trigger reads "Use as Profile Picture" and whose options are labeled with the **actual entity names** of the three entity slots (`runtime.active_ai?.name`, `runtime.active_user?.name`, `runtime.active_fractal?.name`, each falling back to `app.selected_*` and then to placeholder labels; the runtime fractal placeholder id `"active_fractal"` is treated as no-entity). The seed field + Regenerate/Download buttons were reorganized into a uniform 2×2 grid (row 1: Seed · Regenerate; row 2: Profile Picture · Download) with explicit `col-start`/`row-start` placement so the layout holds when Regenerate is absent, and all four cells are `h-12!` full-width so they share identical dimensions. Selecting an option applies the image immediately (`handle_use_as_profile` mirrors `ProfileState.setImage`: `runtime.update_entity(type, String(id), { profile_picture, image })`, type mapping `ai`/`user` → `"character"`, `fractal` → `"fractal"`; source is the canvas `toDataURL` or `state.src`). No confirm step and **no inline status feedback** — the previous design's "profile picture updated…" saving/success/error text was removed at the user's explicit request. Verified: Svelte 5.56.3 client+server compile 0 warnings/0 errors; `get_profile_target`/`profile_options` simulated across 4 state shapes (live entities, fractal placeholder, storyboard-only selections, empty → all disabled); persistence reuses the existing `runtime.update_entity` surface. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**ImagePreview profile-picture promotion (2026-08-03)**: per user request, the storymode image lightbox (`src/ui/molecules/ImagePreview.svelte`) gained a single combined "Use as Profile Picture" control — a bits-ui `Dropdown` in the right metadata panel whose trigger reads "Use as Profile Picture" and whose options are labeled with the **actual entity names** of the three entity slots (`runtime.active_ai?.name`, `runtime.active_user?.name`, `runtime.active_fractal?.name`, each falling back to `app.selected_*` and then to placeholder labels; the runtime fractal placeholder id `"active_fractal"` is treated as no-entity). The seed field + Regenerate/Download buttons were reorganized into a uniform 2×2 grid (row 1: Seed · Regenerate; row 2: Profile Picture · Download) with explicit `col-start`/`row-start` placement so the layout holds when Regenerate is absent, and all four cells are `h-12!` full-width so they share identical dimensions. Selecting an option applies the image immediately (`handle_use_as_profile` mirrors `ProfileState.setImage`: `runtime.update_entity(type, String(id), { profile_picture, image })`, type mapping `ai`/`user` → `"character"`, `fractal` → `"fractal"`; source is the canvas `toDataURL` or `state.src`). No confirm step and **no inline status feedback** — the previous design's "profile picture updated…" saving/success/error text was removed at the user's explicit request. Verified: Svelte 5.56.3 client+server compile 0 warnings/0 errors; `get_profile_target`/`profile_options` simulated across 4 state shapes (live entities, fractal placeholder, storyboard-only selections, empty → all disabled); persistence reuses the existing `runtime.update_entity` surface. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**DevTelemetryBlock telemetry UI enrichment (2026-08-03)**: all data already rode in the telemetry `meta` payloads, so this was purely presentational. MEMORY_FORMATION cards now attribute the forge (`Forged for <entity> · from N turns`, target keys AI_CHARACTER/USER_PERSONA/FRACTAL resolved via `get_entity_name`, which was also extended to map those uppercase director keys), each weaved memory shows a `type` badge + `w<emotional_weight>` + tag chips (`◇ eternal shift` marker). DYNAMICS_DELTA cards were regrouped per-entity (each entity header groups its own dynamics bars + amendments together; `USER_PERSONA` amendments now surface under their own block instead of only ai/fractal) — the per-axis `cause:` attribution lines were removed at user request (causes still surface as ACTIVE DYNAMICS chips), and the "View Raw Meta" button became an `Accordion` matching the DevWing's "View JSON Data". DYNAMICS_DELTA also gained a SIGNAL PROMPTS section (array-or-object normalization) and the ACTIVE DYNAMICS chip tooltip includes the prompt text. Mirror tests extended to cover per-entity grouping + snapshot fallback (11/11 total).

**Follow-up layout pass (user feedback)**: (1) the border rule under each per-entity name header was removed (the `border-b` on the entity `<header>`); (2) each entity's dynamics bars now render in a 2-column grid (`grid grid-cols-2 gap-4`) instead of a single full-width stack — AI_CHARACTER's 4 axes read 2 per column, FRACTAL's 2 axes read 1 per column. Verified: Svelte 5.56.3 client+SSR compile 0 warnings, SSR render confirms per-entity `grid grid-cols-2` containers with the expected bar counts, 11/11 mirror tests pass.

**Legacy atmospheric-signals removal (2026-08-03)**: per user request, the entire unwired `contributors`/`cause`/`signals`/`signal_prompts` system (leftover from an earlier, more complex dynamics engine) was deleted: `compute_deltas` no longer takes `contributors`/`contributorPrefix` or emits `delta.cause`; `capture_dynamics_delta` no longer logs `signals`/`signal_prompts`; `execute_turn`'s snapshot no longer seeds `signals: {}`/`signal_prompts: []` and `final_meta.signals` is gone; `DevTelemetryBlock` lost its ACTIVE DYNAMICS chips + SIGNAL PROMPTS sections (`active_dynamics`, `signal_prompts`, `get_explanation`, the `tooltip` import). Tests updated accordingly (kernel.test.js capture test, prompts.test.js fixture, DevTelemetryBlock mirror tests 11 → 8). NOTE: `DevWing`'s `active_dynamics` is the separate live-profile meter grid and was left untouched. Verified: acorn syntax check on all 4 edited JS files, Svelte 5.56.3 client+SSR compile 0 warnings, SSR render confirms no chips/prompts/cause, 8/8 mirror tests pass. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**Telemetry payload restructure (2026-08-03)**: per user request, the DYNAMICS_DELTA raw-meta dump was reorganized and deduped. `capture_dynamics_delta` now logs a grouped shape — `{ type, entities: { ai: { name, dynamics }, fractal: { name, dynamics } }, deltas, mutations, vectors }` — instead of the flat `ai`/`ai_name`/`fractal`/`fractal_name` scatter, and two redundancy sources are gone: (1) `mutations.*.dynamics_deltas` is stripped (`sanitize_mutations`) because the computed `deltas` array already carries axis/target/old/new/diff; (2) vector `_embedding` (384-dim Float32Arrays, previously JSON-stringified into thousands of keys) plus `_semantic_score`/`_recency_factor` are stripped from telemetry vectors (`sanitize_vectors`; `_relevance` kept because the PAST/FUTURE cards display it). The dead `flags: []` field was dropped from the telemetry too. `DevTelemetryBlock` reads the new shape via backward-compatible fallbacks (`meta.entities?.ai?.dynamics || meta.ai || …`, `meta.entities?.ai?.name || meta.ai_name || …`) so pre-existing session-log entries still render; it also gained a NEW VECTORS display per entity block (`mutations.*.new_vectors`, type badge + w-weight chip, `has_mods` now includes new_vectors so new-vector-only entities get a block). Kernel tests assert the grouped entities shape + sanitization; DevTelemetryBlock mirrors 8 → 10 (entities-payload read, new-vectors-only block). Verified: acorn syntax check, Svelte 5.56.3 compile 0 warnings, SSR render of new + legacy shapes, 10/10 mirror tests, sanitize helpers unit-probed. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**Telemetry updates-shape restructure (2026-08-03, user-authored format)**: the DYNAMICS_DELTA raw-meta dump was rebuilt around the user's exact schema — `{ type: "DYNAMICS_DELTA", updates: { AI_CHARACTER: { name, present_mutations: {physical, non_physical}, eternal_mutations: {physical, non_physical}, vectors: {resolved, new, retrieval}, dynamics: [{axis, old_value, new_value, diff}] }, USER_PERSONA: {...}, FRACTAL: {...} } }`. `kernel.js` now normalizes via `build_update_entry` (renames `present_append_*` → `present_mutations.*`, `new_vectors[].weight` → `emotional_weight`, `resolve_vectors` → `vectors.resolved`, drops `dynamics_deltas`) and `build_retrieval` (AI past+future scored vectors merged into `vectors.retrieval` sorted by `_relevance` desc, embeddings/stats stripped); `compute_deltas` emits `old_value`/`new_value`; entities with no content are omitted so the dump stays lean. `DevTelemetryBlock` normalizes both the new shape and legacy flat shapes into one block model and: (1) the FUTURE/PAST type badges are no longer pills — plain `font-mono text-(--state-dev-accent)` text matching the PHYSICAL/NON-PHYSICAL amendment labels; (2) the "New Vectors" header label was removed (the vector rows remain). `Message.svelte`'s dev-mode gate now also accepts `meta.updates`. Tests: kernel.test.js capture suite rewritten for the updates shape; DevTelemetryBlock mirrors 10 → 12. Verified: acorn syntax, Svelte 5.56.3 compile 0 warnings, SSR render of new + legacy payloads, 12/12 mirror tests, kernel pure functions unit-probed. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**Telemetry observability pass — DYNAMICS_DELTA Q&A + thoughts/trigger_image/id/type (2026-08-03)**: per user questions, answered (a) `"type": "DYNAMICS_DELTA"` is the log-entry discriminator that selects the card's System Update label/branching — it's the kind tag on the telemetry snapshot, kept top-level; (b) the `w{emotional_weight}` chips ("w4 stuff") were removed from both the MEMORY_FORMATION weaved rows and the new-vector rows (the PAST/FUTURE grid's `_relevance` numbers remain — different metric); (c) director `new_vectors` used to carry no `id` because `temporal_engine.create()` generates one at apply time — `apply_state_mutations` now stamps the generated id back onto the mutation record (`v.id = new_vector.id`) so telemetry's `vectors.new[].id` shows it, and the card renders it beside the type; (d) retrieval vectors now always carry `type` — `build_retrieval` stamps the source-array kind (`past`/`future`) per vector so the dump never mixes them; (e) the leading-space/lowercase " c..." content was the director's raw output stored verbatim — content is now trimmed in `create()`/apply_state_mutations and by the telemetry normalizer before storing/displaying (capitalization is left to the LLM); (f) the director's thoughts now ride into the payload as a top-level `thoughts` string (Cognition + Intent + Somatic Tells + Dialogue Direction + Reasoning from `_thought_process`) — the card gained a THOUGHTS section and the raw-meta Accordion shows it automatically; (g) `trigger_image` is always present in the payload (boolean, defaults false) and the card renders a pulsing TRIGGER IMAGE row when true. Tests: kernel.test.js capture suite +2 (thoughts/trigger_image payload, retrieval type-stamping + trigger_image false default) plus an execute_turn integration test asserting director `_thought_process`/`trigger_image` flow into the logged payload; DevTelemetryBlock mirrors 12 → 15 (new-vector id passthrough, retrieval type/id/relevance mapping, legacy retrieval empty-array). Verified: acorn syntax (4 JS files), Svelte 5.56.3 client+SSR compile 0 warnings, SSR render of the new shape (THOUGHTS + TRIGGER IMAGE + RETRIEVED rows + ids, zero w-chips), legacy + MEMORY_FORMATION renders (no w-chips), 15 mirror assertions + 10 kernel probe assertions + 5 temporal apply_state_mutations probes all pass. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**Telemetry card declutter — thoughts underline, ids/relevance, RETRIEVED label, left-border color-coding (2026-08-03)**: per user request, `DevTelemetryBlock`'s DYNAMICS_DELTA card: (1) the divider line under the THOUGHTS header was removed (the `border-b border-(--state-dev-accent)/20 pb-1` on the Thoughts `<header>`, matching the already-borderless per-entity headers); (2) the vector `id` is no longer rendered on new-vector OR retrieval rows, and the retrieval rows' `_relevance` number is gone too — the card now shows type + content only (ids/relevance remain in the payload and the View Raw Meta accordion, so nothing is lost); (3) the "RETRIEVED" label span above the retrieval rows was removed; (4) retrieved-vs-created is now communicated purely via the row's left-border color — retrieval rows keep the gray `border-l-slate-500`, new/created vector rows keep the electric-cyan `border-l-(--state-dev-accent)` (both were already correct; verified, no swap needed). Presentation-only: no data-mapping changes, so the DevTelemetryBlock mirror tests (15) stay untouched and valid. Verified: Svelte 5.56.3 client+SSR compile 0 warnings; SSR render of the new shape confirms zero id/relevance text in the card body (ids/10.9/8.1 still present only inside the raw-meta `<pre>`), no "Retrieved" label, Thoughts header without border-b, retrieval rows gray + new rows cyan, type labels (CSS-uppercased `future`/`past`) intact; legacy + MEMORY_FORMATION branches still render. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**DevTelemetryBlock label column + chat-bubble thoughts margin (2026-08-03)**: per user request, (1) the Thoughts DataBox in the chat bubble (`Message.svelte`, rendered only in dev mode) uses `isProse={true}`, and `DataBox`'s prose styles give every descendant `<p>` a `margin-bottom` via `[&_p]:mb-4` — since the think block is markdown-rendered (`parse_message` → `md.render`), every paragraph in the Thoughts box had a bottom margin. The DataBox now also carries `[&_.think-block-container_p]:mb-0` (higher-specificity descendant selector than DataBox's own `[&_p]:mb-4`), so `<p>` elements inside the thoughts block are margin-free while the rest of the prose styling is unchanged. (2) DevTelemetryBlock's label column is now uniform width: the PHYSICAL / NON-PHYSICAL / ◇ ETERNAL · PHYSICAL / ◇ ETERNAL · NON-PHYSICAL amendment labels and the FUTURE/PAST vector-type labels all share `w-52 shrink-0` — a fixed 208px column sized to the widest label (`◇ ETERNAL · NON-PHYSICAL`), so the content column starts at the same x for every row. (3) Adopted the user's manual edits as the new baseline: `mb-2` removed from the Thoughts, per-entity, NEWLY_WEAVED_MEMORIES, PAST MEMORIES, and FUTURE VECTORS headers; Accordion label "View Raw Meta" → "View Raw Data"; `get_entity_name` and both `blocks.push` sites reformatted multi-line; amendment/new/retrieval rows reformatted to multi-line class attrs; the WEAVED-row tag-chip expression single-quoted. Presentation-only — no data-mapping changes, so the 16 DevTelemetryBlock mirror tests stay valid. Verified: Svelte 5.56.3 client+SSR compile 0 warnings on both components; 6 label spans carry `w-52 shrink-0`; the `[&_.think-block-container_p]:mb-0` override is present; mirror logic probes (new shape incl. eternal fields, legacy fallback, eternal-only entity, name resolution) pass. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**Eternal amendments surfaced + amendment borders electric-cyan (2026-08-03)**: per user request ("change the present and eternal amendment left border to electric cyan… I included that in 'new'"), the PHYSICAL / NON-PHYSICAL amendment rows (from `present_mutations`) switched from gray `border-l-slate-600` to electric-cyan `border-l-(--state-dev-accent)` — matching the new-vector rows, since amendments are created state, not retrieved. Eternal amendments (`eternal_mutations.{physical,non_physical}`) were in the payload but never displayed — they are now mapped into the block model (new + legacy shapes; legacy stays empty) and render as `◇ ETERNAL · PHYSICAL` / `◇ ETERNAL · NON-PHYSICAL` rows with the same cyan left border (the `◇` echoes the MEMORY_FORMATION `◇ eternal shift` tag). Entities carrying only eternal mutations now count as having mods (`has_mods`). Mirror `process_entity_blocks` updated to match; mirrors 15 → 16 (new eternal-mapping test). Verified: Svelte 5.56.3 client+SSR compile 0 warnings; SSR render confirms zero `border-l-slate-600` in the card body, cyan present + eternal amendment rows, gray retrieval + cyan new vectors intact; mirror probes (eternal maps through, empty eternal still omits the entity, present-only still works) pass. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

**DevTelemetryBlock backward-compat removal + thoughts heading strip + label column rework (2026-08-03)**: per user request: (1) the entire legacy telemetry fallback is gone from `DevTelemetryBlock` — the flat-shape branch in `entity_blocks` (entities/ai/deltas/mutations/snapshot/vectors) and the legacy PAST MEMORIES / FUTURE VECTORS grids at the card bottom were deleted; `get_entity_name` now resolves names from `runtime.active_*` names only; (2) `##` markdown headings are stripped from the THOUGHTS display (`display_thoughts` = `/^##\s+.*$/gm` removal + trim) so `## Reasoning` no longer sits under the THOUGHTS label; (3) the label column dropped `w-40`/\`whitespace-nowrap\` for `w-24 shrink-0` (96px), and `NON-PHYSICAL` uses the non-breaking hyphen `&#8209;` (U+2011) so labels wrap at the space — `PRESENT NON-PHYSICAL` → `PRESENT` / `NON-PHYSICAL` — never at the hyphen. `DevTelemetryBlock.test.js` mirrors updated (legacy branch + legacy tests removed; name resolution runtime-only; 16 → 10 tests). Verified: Svelte 5.56.3 client+SSR compile 0 warnings; SSR render of DYNAMICS_DELTA + MEMORY_FORMATION payloads (heading stripped in the card body, present only inside the raw-meta `<pre>`; 6×`w-24` label spans; U+2011 rendered; legacy grids absent); 10/10 mirror tests. **Pending (user-side)**: `npm run verify` → `npm run build` → redeploy.

## 📐 Audit Summary (grounded findings)

| #   | Item                        | Location                                                                                                                                    | Diagnosis                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1A  | Recency/semantic imbalance  | `src/intelligence/temporal.js` `recency_factor` (59-76), `compute_relevance` (89-94), `score` (103-122), `score_async` (160-179)            | `relevance = weight × (1 + semantic) × recency`. Decay exponent `(10-w)/5` multiplies down to ~0.13 (w1) / 0.33 (w5) by ~100 turns, so `recency` (0.13–1.0) swamps the `(1 + semantic)` ceiling (1–2). A fresh irrelevant memory (sem=0, rec=1.0) outranks an old highly relevant one (sem=1.0, rec=0.33).                                                                                             |
| 1B  | Cache cap / FIFO            | `src/intelligence/embeddings.svelte.js` `MAX_CACHE = 500` (26), `embed()` eviction (100-104)                                                | Hard 500 cap + first-key eviction = FIFO, not LRU (no recency refresh on hit). Repeated contexts churn out and get re-embedded.                                                                                                                                                                                                                                                                        |
| 1C  | Cold-load re-inference      | `src/intelligence/embeddings.svelte.js` (`_embedding` transient, 112-118); `src/data/repository.js` `upsert` (102-124) & `update` (148-159) | `ensure_embedding` stores `Float32Array`, but `repository.js` round-trips entities through `JSON.parse(JSON.stringify())`, flattening the typed array to `{"0":…}` — on reload `_embedding.length` is undefined, so every vector re-infers. `prune()` (224-234) also drops embeddings from snapshots.                                                                                                  |
| 2A  | Trailing quote stripping    | `src/platform/transport.js` `sanitize_llm` (25-47, esp. line 44)                                                                            | `.replace(/^["']                                                                                                                                                                                                                                                                                            [ "']$/g, "")`strips a valid closing dialogue quote (`"`/`'`) from generated message ends. |
| 2B  | Abrupt versionchange reload | `src/data/db.js` (60-63)                                                                                                                    | `db.on("versionchange")` closes + `location.reload()` immediately, killing in-memory pointers (`session_driver._active_id`, `runtime.simulation_story_id`, active entities) mid-turn. `kv_settings` restore exists but races in-flight writes.                                                                                                                                                         |

---

## 📋 Execution Plan

### Phase 1 — Scoring recalibration (1A) + sanitizer fix (2A) [low risk, quick wins]

**1A Recalibrate recency against semantics**

- Introduce a single config block `TEMPORAL_SCORING = { SEMANTIC_GAIN, RECENCY_FLOOR, DECAY_SOFTEN }` at top of `temporal.js`.
- New `compute_relevance`: `weight × (1 + SEMANTIC_GAIN × semantic) × max(RECENCY_FLOOR, pow(recency, DECAY_SOFTEN))`.
  - Proposed: `SEMANTIC_GAIN = 3`, `RECENCY_FLOOR = 0.5`, `DECAY_SOFTEN = 0.5`.
  - Effect: old-but-relevant (sem 1.0, age 100, w5) ≈ `5 × 4 × 0.5 = 10` > fresh-but-irrelevant (sem 0, w5) = `5 × 1 × 1 = 5`. Recency becomes a soft bias/tiebreaker, semantic drives retrieval.
- Keep `v._recency_factor` telemetry as the _effective_ softened value so `DYNAMICS_DELTA` stays honest.
- **TDD** (`temporal.test.js`): (1) old high-sem outranks recent low-sem; (2) recency still decides among equal semantics; (3) floor respected; (4) w10 vectors immune (factor 1.0); (5) `_recency_factor` telemetry reflects softened factor.

**2A Preserve closing dialogue quotes**

- Rework `sanitize_llm` line 44: remove the blanket `[ "']$` class strip. Keep `.trim()`, filler-phrase strip, code-fence strip.
- Strip a leading quote only when **unmatched** (no trailing quote) — removes the `"Sure, here is…` artifact while never eating a closing dialogue quote. Paired outer quotes stay intact (rendered as dialogue spans by `wrap_dialogue` anyway).
- **TDD** (`src/platform/transport.test.js`, new): trailing `"`/`'` preserved; unmatched leading quote stripped; paired outer quotes preserved; fences + filler still stripped.
- Audit `src/ui/utils/text.js` `derive_vector_title` (103) — title-only strip, leave unless a test flags it.

### Phase 2 — True bounded LRU cache (1B)

- `embeddings.svelte.js`: raise `MAX_CACHE` to `1500`; make `embed()` a real LRU — on hit, `delete` + re-`set` key (refresh recency); on overflow evict the Map head. Add `embeddings_engine.cacheStats()` (`{size, hits, max}`) for DevWing telemetry.
- **TDD** (`embeddings.test.js`, new): stub pipeline module; assert LRU order, hit-refresh recency, cap enforcement. (Existing `temporal.test.js` mocks this module — keep `embed`/`score_by_semantics` signatures stable.)

### Phase 3 — Persist vector embeddings (1C)

- `embeddings.svelte.js`: add `serialize_embedding(Float32Array) → number[]` and `deserialize_embedding(any) → Float32Array | null` (accept Float32Array, `number[]`, JSON `{"0":…}`; validate `length === EMBED_DIM`; corrupt → `null`). `ensure_embedding` upgrades persisted plain arrays in place.
- `repository.js`: in `upsert` and `update`, before the JSON round-trip, map `entity.past`/`entity.future` `_embedding` → `serialize_embedding`; in `entities.get` and `runtime.sync`, restore via `deserialize_embedding` so vectors hydrate with ready embeddings.
- Schema-compatible (no migration; `_embedding` already persists as a JSON blob — this just makes it round-trip correctly).
- **TDD** (`repository.test.js` / new assertions + `embeddings.test.js`): upsert→get round-trips `_embedding` as Float32Array; invalid embedding hydrates to `null` (re-infer path); snapshot `prune()` opts embeddings out (keep lean).

### Phase 4 — Graceful versionchange resilience (2B)

- `data/db.js`: versionchange handler quiesces instead of hard-reloading — write a synchronous checkpoint `{story_id, round, phase}` via a new `session_checkpoint` helper (tiered: `sessionStorage` → `window.name`; both synchronous and reload-safe; SecurityError caught), close DB, then schedule `location.reload()` on the next tick. Add a once-per-event guard to prevent reload loops.
- `engine/session.svelte.js` / `state/runtime.svelte.js` `sync()`: on boot, restore from checkpoint before falling back to `kv_settings`; clear checkpoint after restore.
- **TDD** (`db.test.js` + `runtime.test.js`): simulate versionchange (fake event emitter) → asserts `db.close()`, checkpoint written, reload scheduled once; boot restore precedence checkpoint > kv_settings.
- **Manual gate**: two tabs, bump schema, old tab reloads and resumes active story + round.

### Delivery per phase

- Run all affected unit tests locally via `vitest run` (user side, no Node here), static review via `browser_eval` where the live shell permits, then `npm run verify` → `npm run build` → hand off every modified file as a download.

---

## 🗺️ Roadmap Log

- [x] memory-engine-tuning-2026-08-03: 1A recency/semantic rebalance (TEMPORAL_SCORING block, `temporal.js:41`) → 1B true-LRU cache (cap 1500, `embeddings.svelte.js`) → 1C embedding persistence (`vectors.js` serialize/deserialize + `repository.js` round-trip).
- [x] engine-stability-fixes-2026-08-03: 2A dialogue-quote preservation (`transport.js` sanitize_llm) → 2B versionchange resilience (`db.js` quiesce + `session-checkpoint.js` + `runtime.svelte.js` boot restore).

## ✅ Delivery Status

All five phases are implemented with TDD coverage:

| Phase | Deliverable                                                                                                                                                                                                       | Tests                                                                            |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 1A    | `TEMPORAL_SCORING` config; `compute_relevance = weight × (1 + 3·semantic) × max(0.5, raw^0.5)`; `_recency_factor` softened telemetry                                                                              | `temporal.test.js` (outrank, tiebreak, floor, w10 immunity, telemetry)           |
| 1B    | `EMBEDDING_CACHE_MAX = 1500`; true LRU (hit refresh, head eviction); `cacheStats()`                                                                                                                               | `embeddings.test.js` (LRU order, refresh, cap, stats)                            |
| 1C    | `serialize_embedding` / `deserialize_embedding` (`@utils/vectors.js`); repository upsert/update/get round-trip; `ensure_embedding` in-place upgrade                                                               | `repository.test.js` (Float32Array round-trip, legacy `{"0":…}`, corrupt→null)   |
| 2A    | `sanitize_llm` strips leading quote only when unmatched; closing dialogue quotes preserved                                                                                                                        | `transport.test.js` (trailing `"`/`'`, paired quotes, lone quote, fences/filler) |
| 2B    | `db.js` versionchange quiesce→close→reload (loop-guarded); `session-checkpoint.js` (sessionStorage→window.name→memory); `boot.js` hook; `runtime.sync()` checkpoint>kv_settings restore + round/phase propagation | `db.test.js`, `session-checkpoint.test.js`, `runtime-sync.test.js`               |

**Pending (user-side):** `npm run verify` → `npm run build` → redeploy. Handoff package delivered 2026-08-03 (`RPGlitch-handoff-2026-08-03.zip`).

### Follow-up fixes (2026-08-03, from `npm run verify` run)

- `engine/boot.js` — the module-scope quiesce registration is now guarded (`typeof set_versionchange_quiesce === "function"`) so importing `@engine` can't hard-crash in test environments with partial `@data` mocks or a stale `@data` barrel.
- `engine/index.js` — barrel now re-exports `save_session_checkpoint` / `load_session_checkpoint` / `clear_session_checkpoint` from `session-checkpoint.js`.
- `ui/organisms/Profile.test.js` — its `@data/db.js` mock now provides `set_versionchange_quiesce` (boot.js imports it via the `@data` barrel).
- `intelligence/embeddings.test.js` — "exposes the configured maximum cache size" resets the cache cap first; `beforeEach` sets cap=4 for the LRU tests, which leaked into the cap assertion (`expected 4 to be 1500`).
