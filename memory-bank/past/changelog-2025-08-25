<!-- path: past/changelog/2025-08-25-lint-test-fixes.md -->

# 2025-08-25 — Lint & Test Fixes

- **Stylelint** cleanup in `apps/rpglitch/scss/index.scss`  
  Split rules, remove duplicates, reorder for specificity, scoped disables only.

- **Jest** fix in `tests/chin-grid.test.js`  
  Disable debounce in tests; replace `toggleAttribute` with explicit set/remove; align with JSDOM expectations.

- **Validation**  
  `npm run lint` → 0 errors  
  `npm test` → all green

> Production logic unchanged; test-only debounce bypass.
