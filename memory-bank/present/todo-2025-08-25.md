<!-- path: present/todo-2025-08-25.md -->

# TODO — 2025-08-25 (from Time MCP)

## Strategy

Maintain hygiene by clearing simple lint warnings and aligning docs with heading rules.

## Tactics

- Remove unused catch bindings in JS files.
- Make tests README start with an H1 heading.
- Demote extra H1 headings in the unified orchestrator guide.

## Operations

- edit `apps/rpglitch/js/index.js` and `apps/rpglitch/js/utils.js`
- edit `tests/README.md`
- edit `memory-bank/forever/unified-orchestrator-mode.md`

## Chin button sync and layout fixes

### Strategy (layout)

Fix chin/top-bar sync, make background gradient static, align profile screen with top bar.

### Tactics (layout)

- Observe chin hidden attribute to toggle button .selected and body.chin-open
- Apply fixed background gradient via CSS
- Constrain #profile-screen within shared max width container

### Operations (layout)

- edit `apps/rpglitch/js/index.js`
- edit `apps/rpglitch/scss/index.scss`

## Remove redundant chin sync calls

### Strategy (refactor)

Rely on MutationObserver for chin/button sync to avoid duplicate logic.

### Tactics (refactor)

- Drop manual calls to `App.ui.syncChinButtons()` and related helpers.

### Operations (refactor)

## Chin button sync cleanup

### Strategy

Simplify chin click handlers to rely solely on MutationObserver updates.

### Tactics

- Remove redundant sync calls in index and profile-router handlers.

### Operations

- edit `apps/rpglitch/js/index.js`
- edit `apps/rpglitch/js/profile-router.js`

## RPGlitch single-file fixes

### Strategy

Restore chin button sync and profile layout while ensuring inline build has no spread/rest typos.

### Tactics

- Fix `normalize` spread calls in `entities.js`.
- Hardcode debounce with rest param fallback.
- Add container styles for `#profile-screen` and ensure background gradient is fixed.
- Rebuild `RPGlitch.html` with MutationObserver chin sync.

### Operations

- edit `apps/rpglitch/js/entities.js`
- edit `apps/rpglitch/js/utils.js`
- edit `apps/rpglitch/scss/index.scss`
- generate `apps/rpglitch/RPGlitch.html`

## Chin wiring hardening (2025-08-25)

### Strategy

Lock chin open/close behavior and keep UI state consistent across buttons, panels, and body.

### Tactics

- Implement App.chin helpers (getButtons, getPanels, open, close, closeAll, sync, init).
- Watch panel hidden changes with MutationObserver to clear selected buttons.
- Ensure Escape closes any open chin and body.chin-open reflects visibility.
- Inline scripts into RPGlitch.html with fixed debounce.

### Operations

- edit `apps/rpglitch/js/utils.js`
- edit `apps/rpglitch/js/index.js`
- generate `apps/rpglitch/RPGlitch.html`
