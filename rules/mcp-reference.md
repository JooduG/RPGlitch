---
description: MCP reference index and pointers to full tool catalogs
tags: [mcp, reference]
---

# MCP Reference (Index)

- Canonical configuration: `build/config/mcp.master.json`.
- Full command/tool catalogs are maintained in the persistent memory for history:
  - See `memory-bank/forever/mcp-reference.md` for the complete, detailed list.

This rule intentionally stays concise and defers exhaustive listings to the memory bank to avoid duplication and drift.

